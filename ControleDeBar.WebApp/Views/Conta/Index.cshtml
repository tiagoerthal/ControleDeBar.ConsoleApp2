﻿@{
    ViewBag.Titulo = "Visualização de Contas";
}

@model VisualizarContasViewModel;

<div class="row g-3 justify-content-center align-items-center px-3 px-md-2">
    <a class="col-md-4 col-lg-2 btn btn-primary" asp-action="Abrir">
        <i class="bi bi-plus"></i>
        Abrir nova
    </a>

    <div class="col">
        <div class="d-flex gap-2">
            <a class="btn btn-outline-primary" asp-action="Index">
                @* <i class="bi bi-plus"></i> *@
                Todas as contas
            </a>

            <a class="btn btn-outline-primary" asp-action="Index" asp-route-status="abertas">
                @* <i class="bi bi-plus"></i> *@
                Contas abertas
            </a>

            <a class="btn btn-outline-primary" asp-action="Index" asp-route-status="fechadas">
                @* <i class="bi bi-plus"></i> *@
                Contas fechadas
            </a>
        </div>
    </div>

</div>


<div class="row g-3 mt-4">
    @foreach (DetalhesContaViewModel c in Model.Registros)
    {
        <div class="col-md-6 col-lg-4 col-xl-3">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">@c.Titular</h5>
                    <p class="card-text mb-0"><strong>Mesa:</strong> #@c.Mesa</p>
                    <p class="card-text mb-0"><strong>Garçom:</strong> @c.Garcom</p>

                    @if (c.EstaAberta)
                    {
                        <span class="d-flex gap-1 justify-content-end">
                            <a title="Gerenciar pedidos" asp-action="GerenciarPedidos" asp-route-id="@c.Id" style="height: 33px; width: 35px;" class="btn btn-primary rounded-circle d-flex justify-content-center align-items-center">
                                <i class="bi bi-cart"></i>
                            </a>
                            <a title="Fechar conta" asp-action="Fechar" asp-route-id="@c.Id" style="height: 33px; width: 35px;" class="btn btn-success rounded-circle d-flex justify-content-center align-items-center">
                                <i class="bi bi-cash-coin"></i>
                            </a>
                        </span>
                    }
                    else
                    {
                        <a title="Detalhes" asp-action="Detalhes" asp-route-id="@c.Id" style="height: 33px; width: 35px;" class="btn btn-primary rounded-circle d-flex justify-content-center align-items-center ms-auto">
                            <i class="bi bi-search"></i>
                        </a>
                    }
                </div>
            </div>
        </div>
    }
</div>